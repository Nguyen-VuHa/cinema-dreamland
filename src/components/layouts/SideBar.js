import React from 'react';

function SideBar() {
    return (
        <div 
            className='fixed top-0 left-0 w-[20%]'
        >
            SideBar
        </div>
    );
}

export default SideBar;
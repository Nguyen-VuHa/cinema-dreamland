
import React, { Suspense } from 'react';

function LayoutOTPVerify({ children }) {
    return (
        <Suspense>
            {children}
        </Suspense>
    );
}

export default LayoutOTPVerify;